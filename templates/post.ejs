<!DOCTYPE html>
<html lang="en-AU">
<head>
  <title><%= post.title %></title>
  <meta property="og:title" content="<%= post.title %> - Lilly Piri" />
  <meta name="description" content="<%= post.headers.description %>" />
  <meta property="og:description" content="<%= post.headers.description %>" />
  <meta property="og:image" content="<%= post.headers.share %>" />
  <meta property="og:url" content="https://lillypiri.com/<%= post.slug %>" />
  <meta name="author" content="Lilly Piri" />

  <meta name="viewport" content="width=device-width, minimum-scale=1.0, maximum-scale=1.0"  />
  <link type="image/png" href="/icon.png" rel="icon" />
</head>

<body>

  <%- include('header') %>

<article>
  <% if (post.headers.format === 'partial') { %>
    <%- post.html %>
  <% } else { %>
    <header>
      <% if (post.headers.icon) { %><img src="<%= post.headers.icon %>" /><% } %>
      <h1><%= post.title %></h1>
      <aside>
      </aside>
    </header>

    <% if (post.headers.banner) { %>
      <figure>
        <img src="<%= post.headers.banner %>" />
        <% if (post.headers.caption) { %>
          <figcaption><%= post.headers.caption %></figcaption>
        <% } %>
      </figure>
    <% } %>

  <section>
    <%- post.html %>
  </section>

  <% } %>

</article>

<%- include('footer') %>

</body>
</html>